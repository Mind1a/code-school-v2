I’m sending my existing project files (HTML, SCSS, JS).
Please convert everything into a **Next.js project** using:

- **Next.js 15+ App Router**
- **TypeScript**
- **Tailwind CSS**
- **React Query** (with Fetch API, no custom hooks, with api.js file)
- **Feature-based folder structure**

---

## 🧭 Routing & Data endpoints (new / required)

Frontend must call the backend endpoints we designed. Use these routes as the source of truth for data fetching:

- Courses list: `GET /courses`
- Single course: `GET /courses/:courseId`
  (contains course meta like `name`, `author`, `sectionCount`)
- Course Table of Contents (TOC): `GET /courses/:courseId/toc`
- Course chapters list: `GET /courses/:courseId/chapters`
- Single chapter (populated with sections and homeworks):
  `GET /courses/:courseId/chapters/:chapterId`
  Example response shape: the **Chapter JSON** we discussed (includes `sections[]` and `homeworks[]`).
- Chapter homeworks list: `GET /courses/:courseId/chapters/:chapterId/homeworks`
- Single homework: `GET /courses/:courseId/chapters/:chapterId/homeworks/:homeworkId`

When creating/updating resources, the corresponding endpoints are:

- `POST /courses` `PATCH /courses/:courseId` `DELETE /courses/:courseId`
- `POST /courses/:courseId/chapters` `PATCH /courses/:courseId/chapters/:chapterId` `DELETE /courses/:courseId/chapters/:chapterId`
- `POST /courses/:courseId/chapters/:chapterId/homeworks` `PATCH /courses/:courseId/chapters/:chapterId/homeworks/:homeworkId` `DELETE /courses/:courseId/chapters/:chapterId/homeworks/:homeworkId`
  (Implement UI forms for create/update only if present in your current HTML or if I ask for them.)

---

## 🧱 Required Routes / Pages (App Router)

Create these pages (file paths shown relative to `app/`):

- `app/page.tsx` → `/` (home)
- `app/about/page.tsx` → `/about`
- `app/courses/page.tsx` → `/courses` (courses overview)
- `app/courses/[courseId]/layout.tsx` → course layout (persistent sidebar with TOC)
- `app/courses/[courseId]/page.tsx` → `/courses/:courseId` (course overview + TOC preview)
- `app/courses/[courseId]/chapters/[chapterId]/page.tsx` → `/courses/:courseId/chapters/:chapterId` (chapter detail: sections + homeworks list)
- `app/courses/[courseId]/chapters/[chapterId]/homeworks/[homeworkId]/page.tsx` → `/courses/:courseId/chapters/:chapterId/homeworks/:homeworkId` (single homework page)

Also include:

- A shared `app/layout.tsx` for global layout + Tailwind globals
- Error & not-found pages where appropriate

---

## 🧩 UI / Behavior requirements

- **Sidebar**:

  - Appears on all course pages using `app/courses/[courseId]/layout.tsx`.
  - Sidebar is a **Client Component** and persistent across chapter/homework navigations so only the main area re-renders when switching subpages.
  - Sidebar shows the course TOC from `GET /courses/:courseId/toc` and highlights current chapter/subchapter.

- **Chapter page** (`/chapters/:chapterId`) must display:

  - `chapterTitle`, `chapterNumber`
  - `sections[]` — each with `subTitle`, `description`, optional `imageUrl`
  - `homeworks[]` summary (id, title, short description) and links to each homework page

- **Homework page** must display:

  - `homeworkNumber`, `homeworkTitle`, `description`, `help`, `exampleCode`, `answer`, `hints[]`, `resources[]`
  - Provide a “Show answer” toggle (answer hidden by default)
  - If `exampleCode`/`answer` are present, show them in a code block component with copy-to-clipboard

- **TOC page / component**:

  - Use `GET /courses/:courseId/toc`
  - Display full course structure (chapter numbers & sub-items)
  - Clicking a TOC item navigates to that chapter or subchapter

- **Data fetching**:

  - Use **React Query** hooks with the Fetch API.
  - Implement hooks like `useCourse(courseId)`, `useCourseToc(courseId)`, `useChapter(courseId, chapterId)`, `useHomework(courseId, chapterId, homeworkId)`.
  - Use optimistic UI for create/update/delete only if forms exist.

- **Accessibility & UX**:

  - Keyboard accessible sidebar, focus outlines, semantic HTML.
  - Mobile responsive: collapsed sidebar on small screens with a toggle.

---

## ⚙️ Deliverables

1. Full Next.js folder structure (App Router + feature-based)
2. `package.json` + `tsconfig.json` + `tailwind.config.js` + `postcss.config.js` + global CSS (Tailwind) so the project runs with `npm install && npm run dev`
3. All `layout.tsx` and `page.tsx` files for the routes above
4. Converted components from your HTML (Header, Footer, Sidebar, CourseCard, ChapterView, HomeworkView, CodeBlock)
5. React Query provider in root + example hooks (with proper typing)
6. API service wrapper (small fetch wrapper that React Query hooks use) with base URL configurable (e.g., `NEXT_PUBLIC_API_BASE`)
7. Example mocks (if backend is not present) — but real calls by default to backend routes above
8. Small README with run instructions and where to change API base URL

---

## 🔁 Conversion Rules (same as before, updated for new data)

- Convert **HTML → TSX** (functional React components)
- Convert **SCSS → Tailwind utility classes** (try to keep visual fidelity)
- Convert **JS → TypeScript** (include proper prop and response typings)
- Replace DOM-manipulation scripts with React hooks/effects
- Sidebar and interactive components must be Client Components (`"use client"`)
- Add short comments for non-trivial logic (data fetching, hydration notes, code blocks)
- Provide a few unit-test friendly helpers where useful (pure functions)

---

## 🧾 Example TypeScript types (expected)

```ts
type Course = {
  _id: string;
  name: string;
  author: string;
  sectionCount: number;
  description?: string;
};

type TocItem = {
  chapterNumber: string;
  title: string;
  shortDescription?: string;
  homeworks: number;
};

type Chapter = {
  _id: string;
  course: Course | string;
  chapterNumber: string; // "2.2"
  chapterIndex: number;
  chapterTitle: string;
  sections: { subTitle?: string; description?: string; imageUrl?: string }[];
  homeworks: {
    _id: string;
    homeworkNumber: number;
    title: string;
    description?: string;
  }[];
};

type Homework = {
  _id: string;
  courseId: string;
  chapterId: string;
  homeworkId: number;
  homeworkTitle: string;
  description?: string;
  help?: string;
  exampleCode?: string;
  answer?: string;
  hints?: string[];
  resources?: { title: string; url: string }[];
};
```

---

## ✅ Final line — ready to start?

Now I’ll send my HTML / SCSS / JS files (one or many). Please respond by converting them into the Next.js + TypeScript + Tailwind + React Query project structure described above.

Ready for my files — start with the main HTML layout and any sidebar HTML.
