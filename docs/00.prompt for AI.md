> I’m now sending you my existing project files (HTML, SCSS, JS).
> Please convert everything into a **Next.js project** using:
>
> - **Next.js 15+ App Router**
> - **TypeScript**
> - **Tailwind CSS**
> - **React Query** (with Fetch API)
> - **Feature-based folder structure**
>
> ---
>
> ### 🧱 My Routing Logic
>
> - Home page → `/`
> - About project → `/about`
> - Courses overview → `/courses`
> - Each course → `/courses/[courseSlug]/` (example: `/courses/htmlCourse/`)
> - Each chapter → `/courses/[courseSlug]/[chapter]/` (example: `/courses/htmlCourse/chapter-1/`)
> - Each subchapter → `/courses/[courseSlug]/[chapter]/[subChapter]/`
>   (example: `/courses/htmlCourse/chapter-1/subChapter-1.1`)
>
> **Sidebar navigation behavior:**
>
> - The sidebar appears on all course pages (`/courses/[courseSlug]/layout.tsx`).
> - When I click on another subchapter (for example, from `1.1` to `1.2`),
>   only the main content area should update — sidebar stays persistent.
> - Sidebar should be a **Client Component**.
>
> ---
>
> ### ⚙️ What You Should Deliver
>
> 1. **Full Next.js folder structure** (App Router + feature-based)
> 2. **All main page and layout files** (`layout.tsx`, `page.tsx`, etc.)
> 3. **Converted React components** from my HTML
> 4. **Tailwind CSS setup** (config + globals)
> 5. **React Query setup** (provider + example hooks)
> 6. **Dynamic routing pages**:
>
>    - `/courses/[courseSlug]/page.tsx`
>    - `/courses/[courseSlug]/[chapter]/page.tsx`
>    - `/courses/[courseSlug]/[chapter]/[subChapter]/page.tsx`
>
> 7. Code should be **ready to run** with `npm install && npm run dev`
>
> ---
>
> ### 🧩 Conversion Rules
>
> - Convert **HTML → JSX / TSX**
> - Replace **SCSS → Tailwind utility classes**
> - Convert **JavaScript → TypeScript**
> - Use React hooks instead of manual DOM manipulation
> - Keep the original **design and layout**
> - Use **clean, modular components**
> - Add short comments where logic or structure is not trivial
>
> ---
>
> ### ⚙️ Stack Summary
>
> - Next.js (App Router)
> - TypeScript
> - Tailwind CSS
> - React Query
> - Fetch API
> - Feature-based folder organization
>
> ---
>
> Now I’ll start sending you the HTML, SCSS, and JS files —
> please analyze each file and respond with the **Next.js + TypeScript + Tailwind + React Query** version that matches this structure and behavior.

ready for code?
